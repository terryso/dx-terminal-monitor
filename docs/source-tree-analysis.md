# DX Terminal Monitor - 源码树分析

## 项目结构

```
dx-terminal-monitor/
│
├── main.py              # 🚀 主入口 - Telegram Bot 命令处理
│                        # - 8 个命令处理器 (start, balance, pnl, positions, etc.)
│                        # - 自动重试机制
│                        # - 用户授权验证
│
├── api.py               # 📡 API 客户端 - Terminal Markets API 封装
│                        # - 7 个 API 方法
│                        # - 异步 HTTP 请求
│                        # - 错误处理
│
├── config.py            # ⚙️ 配置管理 - 环境变量加载
│                        # - Bot Token
│                        # - 允许的用户列表
│                        # - Vault 地址
│                        # - API 基础 URL
│
├── requirements.txt     # 📦 Python 依赖
│
├── start.sh             # ▶️ 启动脚本 (可执行)
│
├── .env                 # 🔐 环境变量 (不提交)
├── .env.example         # 📝 环境变量模板
├── .gitignore           # Git 忽略规则
├── LICENSE              # MIT 许可证
│
├── README.md            # 英文文档
├── README_CN.md         # 中文文档
│
├── docs/                # 📚 项目文档目录
│   ├── index.md
│   ├── project-overview.md
│   ├── architecture.md
│   ├── api-contracts.md
│   ├── development-guide.md
│   └── source-tree-analysis.md
│
├── _bmad/               # BMAD 框架配置
├── _bmad-output/        # BMAD 输出目录
│
├── venv/                # Python 虚拟环境 (不提交)
├── __pycache__/         # Python 缓存 (不提交)
├── bot.log              # 运行日志
└── bot.pid              # 进程 PID
```

## 关键目录说明

| 目录/文件 | 用途 | 重要性 |
|-----------|------|--------|
| `main.py` | Bot 主入口，所有命令处理 | ⭐⭐⭐ 核心 |
| `api.py` | Terminal API 封装 | ⭐⭐⭐ 核心 |
| `config.py` | 配置管理 | ⭐⭐ 重要 |
| `.env` | 敏感配置 | ⭐⭐ 重要 |
| `requirements.txt` | 依赖声明 | ⭐⭐ 重要 |
| `start.sh` | 启动脚本 | ⭐ 辅助 |
| `docs/` | 项目文档 | ⭐ 辅助 |

## 入口点

- **主入口**: `main.py` 中的 `main()` 函数
- **启动方式**:
  - 直接运行: `python main.py`
  - 脚本启动: `./start.sh`

## 文件依赖关系

```
main.py
  ├── config.py (导入配置)
  └── api.py (导入 API 客户端)

api.py
  └── config.py (导入 API 基础配置)

config.py
  └── .env (读取环境变量)
```

## 数据流

```
Telegram 用户命令
       ↓
main.py (命令处理器)
       ↓
api.py (Terminal API 客户端)
       ↓
Terminal Markets API
       ↓
响应格式化 → Telegram 消息
```

## 排除的目录

以下目录不在源码分析范围内：
- `venv/` - Python 虚拟环境
- `__pycache__/` - Python 缓存
- `.git/` - Git 仓库
- `_bmad/` - BMAD 框架配置
